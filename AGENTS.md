# Codex 프로젝트 작업 지침

## 작업 시작

- 이 저장소에서 작업을 시작할 때 `Docs/HANDOFF.md`와 `README.md`를 먼저 읽어 현재 상태와 다음 단계를 확인한다.
- 이전 대화를 기억한다고 가정하지 않는다. 문서와 실제 코드가 다르면 현재 코드 및 사용자의 최신 지시를 기준으로 판단하고 인계 문서를 정정한다.
- 기존 로컬 체크아웃의 변경을 먼저 확인하고 보존한다. 다른 컴퓨터의 미푸시 작업이나 과거 대화 내용을 추측하지 않는다.

## 작업 맥락 유지

- 사용자는 기기를 바꿔도 작업을 이어가기를 원한다. 의미 있는 작업 완료 시 및 기기 전환 요청 시 `Docs/HANDOFF.md`를 갱신한다.
- 최신 요청, 확정된 결정과 이유, 변경한 내용, 실제 실행한 검증과 결과, 미완료 사항, 다음 단계를 간결하게 기록한다.
- 실행하지 않은 검증은 실행하지 않았다고 명시한다. 기존 문서의 테스트 결과를 새 환경의 실행 결과로 표현하지 않는다.
- 인계 문서는 허용된 저장/커밋/푸시 흐름에 포함한다. GitHub 반영이 확인된 경우에만 다른 환경에서 읽을 수 있다고 보고한다. 이 지침 자체가 관련 없는 코드 변경의 푸시나 병합을 승인하지는 않는다.
- 대화 원문 자동 동기화와 프로젝트 요약 저장을 구분한다. 이 문서는 앱 대화 동기화를 설정하지 않는다.
- 이 저장소는 공개되어 있다. 개인정보, 대화 원문, 인증 정보, API 키 및 컴퓨터별 비밀 설정은 기록하지 않는다.

## Unity

- Unity 버전 및 실행 방법은 README와 ProjectSettings를 확인한다.
- Assets 변경 시 필요한 .meta 파일을 함께 관리하고 Library/Temp 등 생성물을 커밋하지 않는다.
- MCP 서버 및 AI 클라이언트 설정은 컴퓨터별로 확인한다.
